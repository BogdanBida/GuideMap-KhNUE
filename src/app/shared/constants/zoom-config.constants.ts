import { environment } from 'src/environments/environment';

export const MAX_ZOOM = 1;
export const MIN_ZOOM = 0.25;
export const DEFAULT_ZOOM_STEP = 0.25;
export const DEFAULT_ZOOM_FACTOR = environment.defaultZoomFactor;
